@model IEnumerable<bài_tập_cộng_thêm_điểm.Models.Grade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GradeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Students)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.GradeName)
            </td>
            <td>
                @Html.DisplayFor(model => item.Students)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Gradeid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Gradeid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Gradeid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
